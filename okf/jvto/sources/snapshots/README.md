# Local upstream snapshots — do not commit

`fetch_snapshots.py` downloads only paths listed in `../../config/upstreams.yaml`.

Snapshots are local curation inputs, not public OKF concepts. They can contain stale, internal, or non-publishable material. The final public bundle must contain only facts independently reviewed against current public sources.
